# StealthTap runtime dependencies (services in docker-compose.yml).
# Model TRAINING deps (xgboost, skl2onnx, onnxmltools, pandas) are in
# requirements-train.txt -- they are not needed to run the pipeline,
# only to (re)build the ONNX artifacts in models/.
faust-streaming>=0.10.16
river>=0.21
redis>=5.0
numpy>=1.26
scipy>=1.12
scikit-learn>=1.4
onnxruntime>=1.17
safetensors>=0.4
pydantic>=2.6
opensearch-py>=2.5
pytest>=8.0
yara-python>=4.5
aiokafka>=0.11
fastapi>=0.115,<0.116
starlette>=0.41,<0.48      # pin: a mismatched starlette silently breaks include_router
uvicorn[standard]>=0.32
asyncpg>=0.30
scapy>=2.6.0
psutil>=5.9          # live-capture interface enumeration (src/capture/interfaces.py)
python-multipart>=0.0.12
# NOTE: torch was removed. ENG-03's DGA branch previously instantiated an
# *untrained* CNN (random weights -> non-deterministic output). It now
# uses the trained `dns` ONNX model via onnxruntime, with a deterministic
# lexical heuristic fallback -- no deep-learning runtime required.
